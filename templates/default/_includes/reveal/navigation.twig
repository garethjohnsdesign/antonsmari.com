<nav class="full reveal tertiary-background-color" id="overlayNavigation" data-reveal data-animation-in="fade-in" data-animation-out="fade-out">
  <div class="grid-y grid-padding-x grid-padding-y align-justify text-center" style="height: 100%">

    <div class="shrink cell text-uppercase">
      <a href="#" class="dark-gray" data-toggle="overlayNavigation">CLOSE</a>
    </div>

    <div class="shrink cell text-uppercase">
      <ul class="vertical menu">
        {% nav category in craft.categories.group('typeOfProject').all() %}
          <li>
           <a href="{{ category.url }}" data-toggle="overlayNavigation">{{ category.title }}</a>
          </li>
        {% endnav %}
        <li><a href="/information" data-toggle="overlayNavigation">Information</a></li>
      </ul>
    </div>

    <div class="shrink cell">
     <ul class="menu align-center dark-gray">
      {% for block in socials.socials.all() %}
      {% switch block.type %}
        {% case "instagram" %}
          <li class="padding-left"><a href="{{block.socialLink}}" target="_blank">Instagram</a></li>
        {% case "vimeo" %}
          <li><a href="{{block.socialLink}}" target="_blank">Vimeo</a></li>
      {% endswitch %}
      {% endfor %}
      </ul>
    </div>

  </div>
</nav>